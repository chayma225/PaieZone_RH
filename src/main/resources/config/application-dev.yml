logging:
  level:
    ROOT: DEBUG
    tech.jhipster: DEBUG
    org.hibernate.SQL: DEBUG
    tn.paiezone.rh: DEBUG

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false
  jackson2:
    serialization:
      indent-output: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/paieZoneRH
    username: paieZoneRH
    password: paiezone
    hikari:
      poolName: Hikari
      auto-commit: false
  liquibase:
    contexts: dev
  jpa:
    properties:
      hibernate:
        # Force Hibernate à traiter les @Lob comme du binaire direct (bytea)
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
  mail:
    host: mail47.lwspanel.com
    port: 587
    username: salim.hamidi@comptazone.com
    password: Ahmed@@@239
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true # Ajouté pour garantir la sécurité sur le port 587
  messages:
    cache-duration: PT1S
  thymeleaf:
    cache: false
  docker:
    compose:
      enabled: false

server:
  port: 8080
  forward-headers-strategy: native

jhipster:
  cache:
    redis:
      expiration: 3600
      server: redis://localhost:6379
      cluster: false
  cors:
    allowed-origins: 'http://localhost:8100,https://localhost:8100,http://localhost:4200,https://localhost:4200'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${jhipster.clientApp.name}-alert,X-${jhipster.clientApp.name}-error,X-${jhipster.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800
  security:
    authentication:
      jwt:
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  mail:
    from: salim.hamidi@comptazone.com # INDISPENSABLE : doit être identique au username
    base-url: http://127.0.0.1:8080
  logging:
    use-json-format: false
